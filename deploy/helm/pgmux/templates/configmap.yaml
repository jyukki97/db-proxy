apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pgmux.fullname" . }}
  labels:
    {{- include "pgmux.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
