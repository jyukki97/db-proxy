apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgmux.fullname" . }}
  labels:
    {{- include "pgmux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: pg
      protocol: TCP
      name: pg
  selector:
    {{- include "pgmux.selectorLabels" . | nindent 4 }}
---
{{- if .Values.metricsService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgmux.fullname" . }}-metrics
  labels:
    {{- include "pgmux.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metricsService.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "pgmux.selectorLabels" . | nindent 4 }}
{{- end }}
---
{{- if .Values.adminService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgmux.fullname" . }}-admin
  labels:
    {{- include "pgmux.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.adminService.port }}
      targetPort: admin
      protocol: TCP
      name: admin
  selector:
    {{- include "pgmux.selectorLabels" . | nindent 4 }}
{{- end }}
